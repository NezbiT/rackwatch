# Dev / homelab MQTT broker (compose profile: mqtt).
# Anonymous is on so HA can subscribe on a trusted LAN.
# Lock this down (password_file + listener TLS) before exposing 1883.

listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
